{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}


{% block body %}

        <div class="container d-flex flex-column align-items-center p-5 border rounded my-5">
                {{ form_start(reservation) }}
                {{ form_row(reservation.status) }}
        
                {{ form_end(reservation) }}
        </div>

       
   
    <a href="{{ path('historique_resa') }}"><i class="fas fa-arrow-left fa-3x"></i></a>   


{% endblock %}
